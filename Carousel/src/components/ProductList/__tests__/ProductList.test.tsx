// tests => 1. render product cards (does it correctly map over)
// tests => 2. does it correctly pasrse the productTitle and pass it through
// tests => 3. does it handle loading, errors, or empty states correctly